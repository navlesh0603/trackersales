name: trackersales
description: "Vunya Sales Tracker - Field Sales and Location Tracking Solution"
publish_to: 'none'

version: 1.0.2+2

environment:
  sdk: ^3.8.1


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.14.0
  geolocator: ^14.0.2
  flutter_polyline_points: ^3.1.0
  google_fonts: ^6.3.2
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  http: ^1.6.0
  table_calendar: ^3.2.0
  flutter_local_notifications: ^20.0.0
  flutter_background_service: ^5.1.0
  provider: ^6.1.5+1
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
